if [ -z "${WAYLAND_DISPLAY}" ]; then
    export XDG_RUNTIME_DIR="/tmp/1000-runtime-dir"
    mkdir $XDG_RUNTIME_DIR
    export XDG_CONFIG_HOME=$HOME/.config
    eval `ssh-agent`
    dbus-launch --exit-with-session start-hyprland
fi
